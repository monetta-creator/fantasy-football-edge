import { Stub } from "@/components/Stub";
export default function Page() {
  return <Stub title="Roster" phase="Phase 1–2 — needs Yahoo OAuth" bullets={["Live roster synced from Yahoo with IR-eligibility flags.", "IR choreography: keep one bench slot open before Tuesday waivers, claim, move to IR, refill.", "Every proposed move shows points gained and requires your tap for the first two weeks."]} />;
}
