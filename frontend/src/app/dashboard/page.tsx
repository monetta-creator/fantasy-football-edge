import { Stub } from "@/components/Stub";
export default function Page() {
  return <Stub title="Dashboard" phase="Phase 1 — needs Yahoo OAuth" bullets={["This week's matchup, projected score, and win probability.", "Monte Carlo lineup optimizer: higher-variance lineup when projected to lose, higher-floor when favored.", "Weekly K and DST streaming from Vegas implied totals."]} />;
}
